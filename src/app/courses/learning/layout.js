export const metadata = {
  title: "Course Learning Portal",
  description: "Private learning access for enrolled students.",
  robots: { index: false, follow: false },
};

export default function CourseLearningLayout({ children }) {
  return children;
}
