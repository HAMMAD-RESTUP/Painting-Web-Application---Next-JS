export const metadata = {
  title: "Secure Course Checkout",
  description: "Complete your course purchase through secure checkout.",
  robots: { index: false, follow: false },
};

export default function CourseCheckoutLayout({ children }) {
  return children;
}
