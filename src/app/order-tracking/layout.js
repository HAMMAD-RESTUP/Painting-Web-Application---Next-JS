export const metadata = {
  title: "Order Tracking",
  description: "Private painting order tracking details.",
  robots: { index: false, follow: false },
};

export default function OrderTrackingLayout({ children }) {
  return children;
}
